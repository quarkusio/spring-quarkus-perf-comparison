name: timestamps helper scripts
scripts:
  start-timestamp:
    - sh: date -u +%FT%TZ
      then:
        - set-state: RUN.output.timing.start

  stop-timestamp:
    - sh: date -u +%FT%TZ
      then:
        - set-state: RUN.output.timing.stop